# Test Input for Pre/Post Processing

This is a simple test file to validate that preprocessing and postprocessing scripts are working correctly.

The content should be processed by the prompt chain after preprocessing scripts run and before postprocessing scripts execute.